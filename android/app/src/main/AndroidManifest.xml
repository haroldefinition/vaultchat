<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
  <uses-permission android:name="android.permission.CAMERA"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
  <uses-permission android:name="android.permission.READ_CONTACTS"/>
  <!-- Legacy storage perm — only used on Android 12 and below.
       Android 13+ uses the granular READ_MEDIA_* perms below. -->
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
  <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
  <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
  <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <!-- WRITE_CONTACTS removed in 1.0.16. Audit confirmed VaultChat only
       READS the contact list via expo-contacts (Contacts.getContactsAsync,
       requestPermissionsAsync, getPermissionsAsync) for the contact-import
       handle-discovery flow. No code path writes back to the OS contact
       store. Removing the permission tightens Play Store quality signals
       and avoids reviewer questions about why a messaging app needs to
       modify the user's address book. -->
  <!-- Legacy storage perm — needed only on Android 9 and below
       (scoped storage on Android 10+ removed the need for this). -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29"/>
  <!-- Phase AAA: Android calling stack -->
  <!-- ConnectionService backbone — lets us register a self-managed phone account via react-native-callkeep -->
  <uses-permission android:name="android.permission.MANAGE_OWN_CALLS"/>
  <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
  <uses-permission android:name="android.permission.READ_PHONE_NUMBERS"/>
  <uses-permission android:name="android.permission.BIND_TELECOM_CONNECTION_SERVICE"/>
  <!-- Background WebRTC + foreground service for in-call audio/video -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL"/>
  <uses-permission android:name="android.permission.WAKE_LOCK"/>
  <!-- Heads-up incoming-call notification while ConnectionService is binding -->
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <!-- Required to open the per-app battery-optimization settings dialog
       so users can whitelist VaultChat from aggressive background killing.
       Without the whitelist, FCM call wake-ups can be silently dropped. -->
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
  <!-- Bluetooth headsets for in-call audio routing (Android 12+ runtime perm) -->
  <uses-permission android:name="android.permission.BLUETOOTH"/>
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="false" android:fullBackupContent="@xml/backup_rules" android:dataExtractionRules="@xml/data_extraction_rules" android:theme="@style/AppTheme" android:supportsRtl="true" android:enableOnBackInvokedCallback="false">
    <!-- Phase AAA: tools:replace forces our values to win the manifest merge.
         react-native-firebase_messaging declares default_notification_color
         with a different default (@color/white), and the merger refuses to
         pick a side without an explicit override. Same defensive fix for
         the icon to prevent future conflicts. -->
    <meta-data android:name="com.google.firebase.messaging.default_notification_color"
               android:resource="@color/notification_icon_color"
               tools:replace="android:resource"/>
    <meta-data android:name="com.google.firebase.messaging.default_notification_icon"
               android:resource="@drawable/notification_icon"
               tools:replace="android:resource"/>
    <meta-data android:name="expo.modules.notifications.default_notification_color" android:resource="@color/notification_icon_color"/>
    <meta-data android:name="expo.modules.notifications.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <meta-data android:name="expo.modules.updates.ENABLED" android:value="false"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="ALWAYS"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0"/>
    <activity android:name=".MainActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.App.SplashScreen" android:exported="true" android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="vaultchat"/>
        <data android:scheme="exp+vaultchat"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.SEND"/>
        <data android:mimeType="text/*"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <!-- Phase AAA: react-native-callkeep ConnectionService.
         Required for self-managed PhoneAccount registration so
         incoming-call UI shows on the lock screen. The
         BIND_TELECOM_CONNECTION_SERVICE permission is what tells
         the OS that VaultChat is allowed to host a managed call. -->
    <service
        android:name="io.wazo.callkeep.VoiceConnectionService"
        android:label="VaultChat Calls"
        android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
        android:foregroundServiceType="phoneCall"
        android:exported="true">
      <intent-filter>
        <action android:name="android.telecom.ConnectionService"/>
      </intent-filter>
    </service>

    <!-- Phase AAA: callkeep's headless background-message bridge.
         Wakes the JS bundle from a fully-killed state when an FCM
         data message arrives so we can synchronously call
         RNCallKeep.displayIncomingCall() before Android kills the
         process. -->
    <service
        android:name="io.wazo.callkeep.RNCallKeepBackgroundMessagingService"
        android:foregroundServiceType="phoneCall"
        android:exported="false"/>
  </application>
</manifest>